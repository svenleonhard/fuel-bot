class UserFuelInformation {
    constructor(fuelType, city, distance) {
        this.fuelType = fuelType;
        this.city = city;
        this.distance = distance;
    }
}

module.exports.UserFuelInformation = UserFuelInformation;